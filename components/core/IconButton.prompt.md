One-line: circular sticker button for a single Lucide icon (cart, close, plus) — same press physics as `Button`.

```jsx
<IconButton label="Cart" variant="red" size={48}><i data-lucide="shopping-bag" /></IconButton>
```

Use `size={48}` on touch surfaces (44px minimum). Icons come from Lucide at 2px stroke.

One-line: renders the supplied wordmark artwork; never typeset "The Coop" by hand.

```jsx
<Logo variant="primary" width={200} assetBase="../../" />
<Logo variant="knockout" width={140} assetBase="../../" />  {/* white letters, black keyline — over red, black or photos */}
```

Clear space around the mark = the height of the bubble "C". Minimum width 90px. `nugs` is the mascot lockup for merch, stickers and hype moments — not for menus or checkout.

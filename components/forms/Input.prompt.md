One-line: the standard text field — 3px black keyline, red focus halo, poster-caps label.

```jsx
<Input label="Name for the order" placeholder="e.g. Dana" hint="We'll shout it out." />
<Input label="Phone" error="Need 10 digits" />
```

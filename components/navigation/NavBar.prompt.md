One-line: the marketing-site header.

```jsx
<NavBar tone="red" assetBase="../../" links={[{value:'menu',label:'Menu'}]} active="menu"
  onNavigate={go} cta={<Button variant="dark" size="sm">FIND THE TRUCK</Button>} />
```

Always sits on a 5px black bottom rule. Never white-on-white; the header is red or black by default.
